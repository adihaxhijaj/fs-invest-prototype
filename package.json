{
  "name": "fs-invest-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "FS Invest — Dardania Complex. Concept prototype (speculative, not an official FS Invest production site).",
  "scripts": {
    "build": "tsx src/build.ts",
    "dev": "tsx src/build.ts && serve dist -l 4173",
    "serve": "serve dist -l 4173",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@fontsource/roboto": "^5.3.0",
    "@fontsource/roboto-mono": "^5.3.0",
    "@pdf-lib/fontkit": "^1.1.1",
    "serve": "^14.2.6",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2"
  }
}
